# YAML file for specifying repositories to fetch issues from

repos:
  - owner: project-chip
    name: connectedhomeip
  - owner: CHIP-Specifications
    name: chip-test-plans
  - owner: CHIP-Specifications
    name: chip-test-scripts
  - owner: CHIP-Specifications
    name: chip-certification-tool

  # Add more repositories if needed
  #- owner: username
  #  repo: reponame
  # ...

